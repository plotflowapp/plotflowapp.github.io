const form=document.getElementById('updates-form');
if(form){form.addEventListener('submit',e=>{e.preventDefault();const email=document.getElementById('email');const consent=document.getElementById('consent');const msg=document.getElementById('message');if(!email.validity.valid){msg.textContent='Please enter a valid email address.';msg.style.cssText='margin-top:12px;font-size:12px;color:#b42318';return}if(!consent.checked){msg.textContent='Please check the box to receive notifications.';msg.style.cssText='margin-top:12px;font-size:12px;color:#9a6700';return}msg.textContent='The signup form is ready for the Brevo form connection.';msg.style.cssText='margin-top:12px;font-size:12px;color:#16865a'}})}
