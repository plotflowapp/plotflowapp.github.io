ai2tex landing package

This folder is designed to be placed inside the existing plotflowapp.github.io repository as:

    /ai2tex/

Do NOT replace the existing root index.html.

Files:
- index.html — ai2tex landing page
- documentation.html — documentation based on PekarShmekar_README.md
- privacy.html — draft privacy page for the optional email signup
- style.css — shared light theme
- script.js — temporary email-form placeholder until Brevo is connected
- assets/logo-light.svg — clean light-background logo variant
- downloads/ — put the real ai2tex.zip here

Before publishing:
1. Add the real ai2tex.zip release.
2. If the actual MAXScript filenames differ, update documentation.html.
3. Replace the Before/After visual placeholders with real screenshots.
4. Connect the optional email form to Brevo and replace the draft privacy text with the final policy.
5. Later, if desired, the /ai2tex/ folder can be moved to its own domain or integrated into the main site without replacing the root index.html.

Pages included: index.html, documentation.html, pricing.html, privacy.html.
